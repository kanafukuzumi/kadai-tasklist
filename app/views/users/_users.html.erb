<% if users.any? %>
<ul>
 <% users.each do |user| %>
 <div>
  <%= user.name %>
 </div>
 <div>
  <p><%= link_to 'View Profile', user_path(user) %></p>
 </div>
 <% end %>
</ul>

<%= paginate users %>
<% end %>